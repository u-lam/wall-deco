import React from "react";
import Layout from "../components/layout";

const Contact = () => (
  <Layout>
    Contact Us => Adding a google iframe form or use emailJS as an added feature
  </Layout>
);

export default Contact;
