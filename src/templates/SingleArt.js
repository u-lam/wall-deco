import React, {useState, useEffect, useContext} from 'react'; 


/*
export const query =  graphql`
  query($slug: String!) {
    pizza: sanityPizza(slug: {current: {eq: $slug}}) {
      name
      id
      image {
        asset {
          fluid(maxWidth:800) {
            ...GatsbySanityImageFluid
          }
        }
      }
      price
      toppings{
        name
        vegetarian
      }
    }
  }
`
*/


const SingleArt = ({ data })=> { 
  return ( 
     <div> 
      
     </div> 
  ) 
} 
export default SingleArt;
